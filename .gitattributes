# Normalize all text files to LF on checkout (Pi runs Linux)
* text=auto eol=lf

# Force LF for shell scripts and Docker files regardless of platform
*.sh text eol=lf
Dockerfile* text eol=lf
docker-compose*.yml text eol=lf
*.sql text eol=lf
